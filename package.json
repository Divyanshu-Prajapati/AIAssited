{
  "name": "dsa-ai-assessment-lab-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root project scripts for DSA AI Assessment Lab",
  "scripts": {
    "install:all": "npm install --prefix Backend && npm install --prefix frontend",
    "dev:server": "npm run dev --prefix Backend",
    "dev:client": "npm run dev --prefix frontend",
    "db:push": "npm run db:push --prefix Backend",
    "db:seed": "npm run db:seed --prefix Backend",
    "build:server": "npm run build --prefix Backend",
    "build:client": "npm run build --prefix frontend",
    "build": "npm run build:server && npm run build:client"
  }
}
